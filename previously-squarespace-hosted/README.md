That's the original content that was manually posed from the andreashoelzl/com.cpwmedspa.squarespace git

Now the CSS and the header injection are a <script> link to the git file itself. It does not inlcude
any data related injections like GA or HotSpot. Those are now still in the Squarespace Header injection.

Because of that, that file underwent a change from being html to js
